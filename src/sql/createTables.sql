CREATE TABLE AA_CRIT_PPHYS (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	NO_REL_CLT VARCHAR(80) NOT NULL,
	NOM_USAGE VARCHAR(80) NOT NULL,
	PRENOM VARCHAR(80) NOT NULL
	); 	